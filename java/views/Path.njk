package com.ringcentral.paths;

import com.ringcentral.Path;
import com.ringcentral.RestClient;
import com.ringcentral.definitions.*;
import com.ringcentral.PathSegment;

public class {{ className }} extends Path {

    public {{ className }} (RestClient restClient, PathSegment parent, String id) {
      this.restClient = restClient;
      pathSegment = new PathSegment(parent, "{{ segment }}", id);
    }

    {% for child in myChildren %}
        {% include "Child.njk" %}
    {% endfor %}

    {% for method in methods %}
        {% for definition in method.definitions %}
            {% include "Definition.njk" %}
        {% endfor %}
    {% endfor %}
}
