{% if child.hasId > -1 %} {# Yes or Maybe #}
    public {{ child.PascalCase }}Path {{ child.PascalCase }}(ID _id)
    {
      return new {{ child.PascalCase }}Path(this, _id);
    }
{% endif %}
{% if child.hasId < 1 %} {# Maybe or No #}
    public {{ child.PascalCase }}Path {{ child.PascalCase }}()
    {
      return new {{ child.PascalCase }}Path(this{% if child.camelCase == 'account' or child.camelCase == 'extension' %}, "~"{% endif %});
    }
{% endif %}
